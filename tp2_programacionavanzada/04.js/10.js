// Crea un array de palabras y ordénalo alfabéticamente usando sort.

const palabras = ['manzana', 'pera', 'banana', 'naranja', 'anana'];

const palabrasOrdenadas = palabras.sort();

console.log(palabrasOrdenadas); 

